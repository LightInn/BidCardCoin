<UserControl x:Class="BidCardCoin.Vue.CRUD.AddMotClefView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BidCardCoin.Vue.CRUD"
             mc:Ignorable="d">
    <Grid Name="grigrid" Margin="0,0,0.300,0" VerticalAlignment="Top">
        <StackPanel>


            <Label Content="Mot Clef" FontSize="20" Foreground="#F8F8F8" />
            <TextBox Name="InputMotClef" Height="20" />


            <DockPanel>
                <Button Click="CreateMotClef">
                    <Border
                        CornerRadius="8" Background="Aqua">
                        <TextBlock>
                            Ajouter Mot Clef
                        </TextBlock>
                    </Border>
                </Button>
                <Button>Anuller</Button>
            </DockPanel>
        </StackPanel>
    </Grid>

</UserControl>